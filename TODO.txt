GAME :
_ Deck remaining
_ Card states [glow] (charge, taunt, confused, stealth, idle != sleeping, not enough MP ...)
_ Surrender
_ CSS clean


COMPAT :

_ Game :
Clickable elements
button text (wordbreak ?) (timer toolarge), timer outline,
Player MP 
Player name background



MISC :
_ Clean code
_ Dics file
_ Clean assets
_ Comments
_ Disable zoom














################################################################################################
SQL :

CREATE TABLE played_cards (
	cardID INT PRIMARY KEY,
	times_played BIGINT
);

DROP TABLE played_cards;

SELECT * FROM played_cards;





PHP:

<?php
    foreach ($data["stats"] as $card) {
        ?>
        <div><?= $card["cardid"] ?> / <?= $card["times_played"] ?> / <?= $card["popularity"] ?></div>
        <div><?= var_dump($data["cards"][$card["cardid"]-1]) ?></div>
        <?php
    }        
    ?>